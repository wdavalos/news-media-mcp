{
  "title": "Input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool",
      "type": "string",
      "description": "MCP tool to call",
      "default": "search_news",
      "prefill": "search_news",
      "editor": "select",
      "enum": ["search_news", "get_article", "get_top_headlines"]
    },
    "params": {
      "title": "Params",
      "type": "string",
      "description": "Tool parameters as JSON string",
      "default": "{}",
      "prefill": "{}",
      "editor": "textfield"
    }
  },
  "required": ["tool"]
}