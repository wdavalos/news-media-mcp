{
  "title": "Input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool",
      "type": "string",
      "description": "MCP tool to call: search_news, get_article, get_top_headlines",
      "default": "search_news",
      "prefill": "search_news",
      "editor": "select",
      "enum": ["search_news", "get_article", "get_top_headlines"]
    },
    "params": {
      "title": "Parameters",
      "type": "object",
      "description": "Tool-specific parameters",
      "default": {},
      "prefill": {},
      "editor": "json",
      "properties": {}
    }
  },
  "required": ["tool"]
}